import { ResetPasswordService } from './reset-password.service';

describe('ResetPasswordService', () => {
  it('should create an instance', () => {
    expect(new ResetPasswordService()).toBeTruthy();
  });
});
