import { UploadDataService } from './upload-data.service';

describe('UploadDataService', () => {
  it('should create an instance', () => {
    expect(new UploadDataService()).toBeTruthy();
  });
});
